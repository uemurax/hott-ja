name: build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    container: tuemura00/texlive

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        uses: ./.github/actions/build
