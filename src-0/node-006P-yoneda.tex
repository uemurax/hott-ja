\documentclass[index]{subfiles}

\begin{document}

\begin{myBlock}{006P}{myDefinition}
  関数外延性を仮定する。
  \myInlineMath{i}を階数、
  \myInlineMath{C \myElemOf \myPreCat{i}}を前圏とする。
  \myNewTerm[よねだうめこみ]{米田埋め込み}(Yoneda embedding)
  \myInlineMath{\myYoneda{C} \myElemOf
    \myFunctor{C}{\myPresheafCat{C}}}を
  \myRef{006R}と\myRef{006M}で\myInlineMath{\myCatMapFun{C}}に対応するものと定義する。
\end{myBlock}

\end{document}