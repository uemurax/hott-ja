#lang typed/racket

(require "core.rkt")

(provide cocone
         cocone:vertex
         cocone:in)

(define cocone (const "Cocone"))
(define cocone:vertex (const "Vertex"))

(define cocone:in (const "in"))
